using System;

class Desafio {
    static void Main() {
        //int quilometros = Int32.Parse(Console.ReadLine()); Argumento original do teste
        int quilometros = 30;
        //int minutos = quilometros * 1; // Cálculo original do teste
        int minutos = quilometros * 2; // Digite aqui o calculo dos minutos
        Console.WriteLine(minutos + " minutos");
    }
}